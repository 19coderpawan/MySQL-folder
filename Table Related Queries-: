1. Update-: (to update the existing rows)
    syntax-: 
              UPDATE Table_name
              SET col1=val1,col2=val2
              WHERE condition;
     example-:
              UPDATE Student
              SET grade="O"  -- this will set the existing grade to value "O" whose value earlier was "A".
              WHERE grade="A"
             
2. Delete-: (to delete the existing rows)
    syntax-:
            DELETE table_name
            WHERE condition;
    example-:
            DELETE STUDENT
            WHERE marks<50; -- delete the data of all students who scored less than 50.
          {NOTE-: BE careful when deleting the data .}

3. ADD COLUMN:- (add a new column to the existing table).        
    syntax-:
            ALTER TABLE TABLE_NAME
            ADD COLUMN COL_NAME DATATYPE CONSTRAINT;
4. DROP COLUMN:- (DROP/DELETE THE COLUMN FROM THE EXISTING TABLE).
    syntax:-    
            ALTER TABLE TABLE_NAME
            DROP COL_NAME;
5. RENAME TO-: (USED TO RENAME THE EXISTING THE TABLE NAME).
    syntax-:
            ALTER TABLE TABLE_NAME
            RENAME TO NEW_NAME;
6. CHANGE COLUMN-: (USED TO RENAME THE EXISTING THE COL_NAME).
    syntax-:
            ALTER TABLE TABLE_NAME
            CHANGE COLUMN OLD_NAME NEW_NAME NEW_DATATYPE NEW_CONSTRAINT;
7. MODIFY COLUMN-: (USED TO MODIFY/UPDATE THE DATATYPE AND CONSTRAINT).
    syntax-:
            ALTER TABLE TABLE_NAME
            MODIFY COL_NAME NEW_DATATYPE NEW_CONTRAINT;
8. TRUNCATE-: (USED TO DELETE TABLE DATA).--->REMEMBER NOT THE ENTIRE TABLE JUST THE DATA OF THE TABLE.
    syntax-: 
            TRUNCATE TABLE TABLE_NAME;
