1.Update-: (to update the exisiting rows)
    syntax-: 
              UPDATE Table_name
              SET col1=val1,col2=val2
              WHERE condition;
     example-:
              UPDATE Student
              SET grade="O"  -- this will set the exisiting grade to value "O" whose value earlier was "A".
              WHERE grade="A"
             
